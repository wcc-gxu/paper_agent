# Paper Agent v3 — 产品规格说明

> 个人科研助理 Agent | 2026-06-14

---

## 1. 产品概述

Paper Agent v3 是一个**24/7 常驻后台的个人 AI 科研助理**。它不是一个"搜索论文的工具"，而是一个具备记忆、能主动发现知识、能自主决策的终身 Agent。

### 1.1 一句话描述

> 你的私人科研大脑——记住你研究过的一切，主动发现你该知道的东西。

### 1.2 产品形态

```
┌──────────────────────────────────────────┐
│              iOS 客户端                   │
│   对话界面 · 论文浏览 · 知识库 · 文件管理  │
├──────────────────────────────────────────┤
│         WebSocket (全双工实时通道)         │
├──────────────────────────────────────────┤
│         Agent 守护进程 (24/7 常驻)         │
│   理解需求 → 制定方案 → 执行调研 → 沉淀知识│
├──────────────────────────────────────────┤
│  搜索源 · PDF下载 · 转换 · 索引 · 综述     │
├──────────────────────────────────────────┤
│  SQLite + ChromaDB + Redis + 文件系统      │
└──────────────────────────────────────────┘
```

---

## 2. 目标用户

| 优先级 | 用户群 | 核心诉求 |
|--------|--------|----------|
| P0 | 研究生/博士生 | 大量读论文、写综述、追踪前沿 |
| P0 | AI/技术工程师 | 快速了解技术领域，获取权威技术资料 |

---

## 3. 核心场景

### 场景 1：文献调研与筛选
**输入**：用户描述研究方向
**Agent 行为**：澄清需求 → 制定方案 → 多源搜索 → LLM 评估相关性 → 展示结果
**产出**：结构化论文列表 + 相关性评分 + 推荐理由

### 场景 2：文献综述生成
**输入**：确定的研究主题
**Agent 行为**：批量搜索 → 下载 PDF → 转换 MD → 向量索引 → 知识提取 → LLM 生成综述
**产出**：投稿级 Markdown 综述 + BibTeX 引用 + 知识卡片

### 场景 3：每日前沿追踪
**输入**：用户研究方向订阅（一次性设置）
**Agent 行为**：每天自动搜索 → 发现新论文 → 分析相关性 → 主动推送
**产出**：每日论文推送 + 摘要 + 推荐阅读理由

### 场景 4：论文精读与提炼
**输入**：用户选定的论文
**Agent 行为**：提取核心贡献 → 方法 → 数据集 → 局限 → 批判性分析
**产出**：结构化知识卡片 + 引用网络分析

### 场景 5：技术资料搜集（Phase 2）
**输入**：GitHub 仓库 / 官方文档 / Stack Overflow 等
**Agent 行为**：网页采集 → MD 转换 → 分类入库 → 语义检索
**产出**：结构化技术知识库

### 场景 6：入库进度查看
**输入**：用户确认调研计划后
**Agent 行为**：启动子Agent → 子Agent 编排入库全流程（搜索→评估→下载→转换→索引→排名→综述）→ 每步实时写入 JSON 日志
**iOS 展示**：入库进度页面实时显示当前阶段、每篇论文处理状态（下载中/已转换/已索引/失败原因）、预计剩余时间
**产出**：完整入库结果 + 结构化进度日志（task .jsonl 文件）

---

## 4. 产品特性

### 4.1 完全主动的智能助理

- **请求驱动**：用户提问 → Agent 响应
- **订阅驱动**：每天自动检查订阅方向的新论文
- **主动发现**：分析用户画像，主动推荐热点方向和相关论文
- **记忆管理**：自动清理无用信息，保存关键知识，按需请求用户确认

### 4.2 终身记忆

| 记忆层级 | 生命周期 | 内容 |
|----------|----------|------|
| 短期记忆 | 当前对话窗口 | 最近 N 轮完整对话 |
| 中期记忆 | 当前任务 | 任务进度、中间结果、检查点 |
| 长期记忆 | 永久 | 研究偏好、重要发现、知识库 |
| 元记忆 | 永久 | 策略有效性、错误模式、用户偏好 |

### 4.3 可控透明的执行

- **Plan-then-Execute**：先生成方案 → 用户确认 → 逐步执行
- **子Agent 后台执行**：用户确认后，子Agent 接管入库全流程，主 Agent 继续对话
- **实时进度**：每步执行状态实时推送 iOS；每篇论文处理状态独立可查（下载中/已下载/转换中/已索引/失败及原因）
- **可暂停/恢复**：随时暂停任务，改参数后继续
- **可回溯**：完整操作日志（结构化 JSONL），LLM 决策全程可审计

### 4.4 流式全双工交互

- LLM 思考过程逐 token 实时展示
- 工具调用过程透明可见
- iOS 断开后 Agent 继续执行，重连时回放事件

---

## 5. 用户完整流程

```
用户: "帮我调研自动驾驶安全方向"
    │
    ▼
┌─ 阶段1: 澄清 (Agent 主动) ────────────────┐
│ Agent: "安全是指功能安全(safety)还是        │
│         AI安全(security)？近几年的论文？"   │
│ 用户: "AI安全，近3年"                      │
└──────────────────────────────────────────┘
    │
    ▼
┌─ 阶段2: 方案生成 ─────────────────────────┐
│ Agent 生成并展示:                          │
│   - 3 个子任务（对抗攻击/鲁棒性/形式验证）  │
│   - 搜索策略（广度优先）                    │
│   - 预期产出（50+ 论文综述）                │
│ 用户: "确认"                               │
└──────────────────────────────────────────┘
    │
    ▼
┌─ 阶段3: 执行 (逐步, 实时进度) ─────────────┐
│ ✅ Step 1/8: 搜索对抗攻击 - 找到 23 篇      │
│ ✅ Step 2/8: 搜索鲁棒性 - 找到 31 篇        │
│ ⏳ Step 3/8: 搜索形式验证 - 搜索中...       │
│ ⬜ Step 4/8: LLM 评估相关性                 │
│ ...                                        │
└──────────────────────────────────────────┘
    │
    ▼
┌─ 阶段4: 验证与重试 ───────────────────────┐
│ Step 1: 23篇 → 评估 → 18篇相关 ✅          │
│ Step 3: 5篇 → 太少 ❌ → 放宽关键词重搜     │
│   → 重新搜索 → 找到 15篇 ✅                │
└──────────────────────────────────────────┘
    │
    ▼
┌─ 阶段5: 汇总产出 ─────────────────────────┐
│ 综述报告: ~/papers/outputs/proj_xxx/survey.md│
│ 引用文件: ~/papers/outputs/proj_xxx/refs.bib │
│ 知识库: 45 个结构化知识卡片                   │
└──────────────────────────────────────────┘
```

---

## 6. 交互模型

### 6.1 对话模式

- **iOS 客户端**：纯 WebSocket 全双工通道
- **消息类型**：用户消息 / 工具执行结果 / 控制指令
- **实时反馈**：LLM 逐 token 输出 + 工具调用进度 + 阶段状态

### 6.2 离线行为

- 用户断开后 Agent 继续执行未完成任务
- 事件缓存在 Redis，重连后回放
- 主动推送通过 APNs 通知 iOS

### 6.3 多项目支持

- 论文研究项目（paper_research）：arXiv, Semantic Scholar, PubMed 等
- 技术文档项目（tech_research）：GitHub, 官方文档, Stack Overflow 等（Phase 2）
- 跨项目全局搜索

---

## 7. 输出产物

| 产物 | 格式 | 位置 |
|------|------|------|
| 文献综述报告 | Markdown | `~/papers/outputs/{project_id}/survey.md` |
| BibTeX 引用 | .bib | `~/papers/outputs/{project_id}/references.bib` |
| 论文元数据 | JSON | `~/papers/outputs/{project_id}/metadata.json` |
| 论文全文 | Markdown | `~/papers/markdown/{project_id}/*.md` |
| 论文 PDF | `~/papers/papers/{source}/{year}/{author}_{title}.pdf` |
| 知识卡片 | SQLite + ChromaDB | 语义可检索 |
| 操作日志 | JSONL | `~/.paper_search/logs/agent.log` |
| 任务进度日志 | JSONL | `~/.paper_search/logs/tasks/{task_id}.jsonl` |

---

## 8. 与竞品的差异

| 维度 | Elicit | Perplexity | 本产品 |
|------|--------|------------|--------|
| 端到端自动化 | 部分 | 部分 | ✅ 搜→下→转→索→提取→综述全自动 |
| 知识沉淀 | ❌ 每次从零 | ❌ | ✅ 跨项目累积，越用越聪明 |
| 质量控制 | 无 | 生成后无验证 | ✅ Plan-then-Execute + 验证重试 |
| 透明度 | ❌ 黑盒 | ❌ 黑盒 | ✅ 每步可见、可暂停、可回溯 |
| 24/7 主动 | ❌ | ❌ | ✅ 订阅推送 + 主动发现 |
| 长期记忆 | ❌ | ❌ | ✅ MemGPT 完整 4 层记忆 |

---

> 版本: v1.1 | 新增入库进度查看场景与进度日志产物
